## Scripts Guide

This folder contains Docker start and stop scripts for local development:

- macOS: `start-mac.sh`, `stop-mac.sh`
- Linux: `start-linux.sh`, `stop-linux.sh`
- Windows: `start-windows.bat`, `stop-windows.bat`

Each script controls the same root `docker compose` application lifecycle.